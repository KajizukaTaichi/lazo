(define (fizzbuzz n)
    (cond
        ((= 0 (% n 15)) "FizzBuzz")
        ((= 0 (% n 3)) "Fizz")
        ((= 0 (% n 5)) "Buzz")
        (true n)))

(for (range 1 101)
    (lambda (i) (print (fizzbuzz i) new-line)))
